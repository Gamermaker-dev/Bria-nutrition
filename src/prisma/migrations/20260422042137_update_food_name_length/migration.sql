BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[food] ALTER COLUMN [name] NVARCHAR(800) NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
